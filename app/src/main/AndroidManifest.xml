<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplicationkk">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplicationkk"
        tools:targetApi="31">

        <!-- Activité principale : Welcome -->
        <activity android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Menu principal -->
        <activity android:name=".MainActivity" android:exported="false"/>

        <!-- Ajout d'une tâche -->
        <activity android:name=".AjoutActivity" android:exported="false"/>

        <!-- Liste des tâches -->
        <activity android:name=".ListeActivity" android:exported="false"/>
    </application>

</manifest>
